services:
  api:
    build:
      context: ./server
    env_file:
      - ./server/.env
    environment:
      PORT: 3001
    restart: unless-stopped

  web:
    build:
      context: .
    ports:
      - "${WEB_PORT:-8080}:80"
    depends_on:
      - api
    restart: unless-stopped
